<p id="notice"><%= notice %></p>
<div class="course">
  <div class="table-responsive"> 
    <table class="table table-striped">
      <thead>
        <tr>
          <th>Chapter</th>
          <th>Chapter Details</th>
        </tr>
      </thead>
      <tbody>
        <tr>
	        <td><%= @chapter.name %></td>
	        <td><%= @chapter.description %></td>
	      </tr>
      </tbody>
    </table>
  </div>
</div>

<%= link_to 'Back to Course', courses_path, :class => 'btn btn-primary' %><br><br>
<% if current_user.role == 'admin' %>
<%= link_to 'Back to Chapter', chapters_path, :class => 'btn btn-primary' %>
<% end %>
