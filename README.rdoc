== README

Metexim Application is a Simple Course application for a Educational Institute.

* Ruby and Rails version
  2.0.0 and 4.1.1
* Database creation
  MySQL and Postgres(Production)

* Run Instructions

git clone https://github.com/ramisn/metexim_course.git

bundle install

rake db:create
rake db:migrate
rake db:seed

rails server
